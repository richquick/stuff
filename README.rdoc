== README

We're using:

* SCSS
* JQuery (1.x for IE < 9)
* jQuery 2.x for modern browsers
* Require JS
* Jasmine (for testing)
* NPM
  - Grunt (For minimization & deployment)

Note:

* The framework uses Rails / Ruby for routing & convenience when developing, but it's not a dependency

